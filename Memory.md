# KNSoft.ZPigeon 项目记忆

本文件只记录跨会话长期有效的项目背景、编码约束和协作约定。系统设计见 `Design.md`，实施状态和下一步见 `Progress.md`。

## 项目定位

- KNSoft.ZPigeon 是 Windows 远程管理软件，目标功能包括文件、软件、进程、服务、事件日志、注册表和远程终端管理。
- 软件只面向合法远程管理场景，不提供隐蔽、注入、绕过或监控安全软件等功能。
- Client 主动连接并认证 Server；认证成功后 Server 对 Client 拥有完整管理能力，Client 不建立第二套操作级授权或本地策略系统。
- SDK 使用纯 C；安全性和效率是最高优先级，稳定性、代码量、运行时开销和 C ABI 均应保持精简。
- 最低系统版本为 Windows 10。应主动使用 Windows 10 及以上系统能力改善安全性、效率和使用体验，不为更低版本系统增加兼容分支、回退实现或维护负担。

## 编码硬规则

- C 代码、头文件布局、命名、SAL、括号和返回状态处理以 `KNSoft.MakeLifeEasier` 为代码风格基线；ZPigeon 使用自己的 `Zp` 前缀，不另建一套风格。
- 代码保持极简。不写无实际必要性的代码、封装、分支、变量、字段、初始化或注释；每一行代码都必须服务于已确认的功能、正确性、安全性或可验证的性能需求。
- 不为了兜底给输出参数预置初值。只在 API 契约、成功路径或安全性明确要求时写入输出参数。
- 不背负未发布版本、旧系统或错误设计的兼容包袱；不增加兜底、自动修复、静默降级或猜测调用方意图的逻辑，错误按明确契约直接返回。
- 在保证安全性和正确性的前提下，尽量贴近底层实现；优先使用 KNSoft.NDK 提供的 NT 层定义和系统函数，避免无意义的 Win32 或自建包装层。
- 优先复用 KNSoft.MakeLifeEasier 已有函数，不在 ZPigeon 重复实现等价通用能力。
- 热路径避免不必要的堆分配、内存复制、编码转换、间接调用和锁；Buffer 生命周期和异步回调语义必须明确。
- 不预先实现未确认的扩展能力，不为假设中的未来需求添加预留字段或兼容代码。

## KNSoft.MakeLifeEasier 协作规则

- 开发 ZPigeon 时持续检查新函数是否具有独立于 ZPigeon 的 common library 价值。
- KNSoft.MakeLifeEasier 可以按项目需要扩展；若新能力具有独立于 ZPigeon 的 common library 价值，编码前必须先询问 Owner，只有取得明确确认后才能实施。
- 经确认后，先在 `E:\KNSoft\KNSoft.MakeLifeEasier` 实现和验证，再把对应变更同步到 ZPigeon 使用的 KNSoft.MakeLifeEasier package。
- 未经确认，不修改 KNSoft.MakeLifeEasier 本地仓库或 ZPigeon 中的依赖包，也不复制一份等价通用实现以规避确认。

## 文档分工

- `Design.md`：系统架构、协议、身份和安全模型、API 方向及待细化的设计问题。
- `Progress.md`：当前阶段、已完成工作、下一步和阻塞项。
- `Memory.md`：长期项目背景、编码约束和协作约定，不重复保存具体设计正文。
